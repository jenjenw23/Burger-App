<li>
	{{burger_name}}

	<button class="change-state" data-id="{{id}}" data-newstate="{{devoured}}">
		{{#if devoured}}Nom nom nom!{{else}}Time to eat!{{/if}}
	</button>

	<!--<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>-->
</li>
